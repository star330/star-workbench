# 【星光不负赶路人】免费发布说明

这个工作台是静态 PWA，发布不需要服务器，也不需要数据库。只要把整个 `xingguang-workbench` 文件夹上传到静态网页托管平台，就可以获得一个可访问的网址。

## 是否需要花钱

一般不需要花钱。

适合个人使用的免费方案：

- GitHub Pages：免费，适合长期保存和公开访问。
- Cloudflare Pages：免费，访问速度通常较好。
- Netlify：免费，拖拽上传最简单。
- Vercel：免费，适合静态项目和前端项目。

可能产生费用的情况：

- 你想购买自己的独立域名，例如 `xingguang.com`。
- 访问量特别大，超过免费额度。
- 需要云数据库、登录账号、多设备同步等后端能力。

目前这个版本不需要这些付费能力，数据保存在你打开网页的浏览器本地。

## 推荐方式一：Netlify 拖拽发布

这是最简单的方式。

1. 打开 Netlify 官网并注册账号。
2. 进入部署页面，选择拖拽上传。
3. 把整个 `xingguang-workbench` 文件夹拖进去。
4. 等待部署完成，Netlify 会生成一个网址。
5. 用手机打开这个网址，在浏览器菜单中选择“添加到主屏幕”。

注意：上传时必须包含 `index.html`、`manifest.webmanifest`、`sw.js`、`styles.css`、`app.js`、`assets`、`_shared` 这些文件和文件夹。

## 推荐方式二：GitHub Pages

适合你想长期维护版本。

1. 注册或登录 GitHub。
2. 新建一个仓库，例如 `xingguang-workbench`。
3. 上传 `xingguang-workbench` 文件夹里的全部内容。
4. 进入仓库 `Settings`。
5. 找到 `Pages`。
6. Source 选择 `Deploy from a branch`。
7. Branch 选择 `main`，目录选择 `/root`。
8. 保存后等待几分钟，GitHub 会生成一个访问地址。
9. 用手机打开该地址，选择“添加到主屏幕”。

## 推荐方式三：Cloudflare Pages

适合希望访问速度更稳定。

1. 注册或登录 Cloudflare。
2. 进入 Workers & Pages。
3. 选择 Pages。
4. 可以连接 GitHub 仓库，也可以使用直接上传方式。
5. 构建命令留空。
6. 输出目录填写 `/` 或留空，取决于平台界面提示。
7. 部署完成后，用手机访问生成的网址并添加到主屏幕。

## 发布后如何使用

- 首页打开后，浏览器会缓存文件，后续可以离线打开一部分内容。
- 新增的热点、题目、错题、申论解析和知识导入默认保存在当前浏览器本地。
- 如果你换手机、换浏览器或清除缓存，历史数据可能丢失。

## 后续可升级方向

如果以后想做得更像真正的 App，可以继续升级：

- 添加数据导出和导入功能。
- 添加账号登录和云端同步。
- 接入每日时政热点自动更新。
- 接入真题材料库。
- 增加申论材料段落标注工具。

这些升级中，导出导入功能仍然可以免费做；账号登录、云端同步、自动更新一般需要后端或第三方服务，可能涉及配置成本或少量费用。
